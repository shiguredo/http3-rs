[package]
name = "ngtcp2-sys"
version = "0.0.0"
edition = "2024"
rust-version = "1.88"
build = "build.rs"
publish = false
links = "ngtcp2"

[lib]
doctest = false

[features]
# bindings.rs を再生成するための feature
# 通常は生成済みの bindings.rs を使用し、ngtcp2 のバージョンを上げたときに再生成する
# 使用方法: cargo build -p ngtcp2-sys --features overwrite
overwrite = ["dep:bindgen"]

[build-dependencies]
shiguredo_cmake = "4.3"
shiguredo_toml = "2026.1.0"
# overwrite feature でのみ使用 (通常ビルドでは libclang 不要)
bindgen = { version = "0.72", optional = true }

[dependencies]
libc = "0.2"
aws-lc-sys = { version = "0.39", features = ["ssl"] }

# 以降は外部ライブラリビルド用のメタデータ
# (この内容は build.rs の中からのみ参照される)
[package.metadata.external-dependencies]
ngtcp2 = { git = "https://github.com/ngtcp2/ngtcp2", branch = "webtransport" }
